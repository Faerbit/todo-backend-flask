machine:
    python:
        version: 3.4.2

dependencies:
    pre:
        - pip install -r requirements.txt

test:
    override:
        - todo/test_main.py

deployment:
  production:
    branch: master
    heroku:
        appname: todo-backend-flask
